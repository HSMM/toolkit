apiVersion: 1

# Все алерты по умолчанию идут в ops-email.
# Тонкая маршрутизация (по severity или тегам) — добавляется в matchers.
policies:
  - orgId: 1
    receiver: ops-email
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
